import React from 'react'
import { IUser } from '../../interfaces/IUser'
import './Blocked.css';

const Blocked = ({ data }: { data: IUser | undefined }) => {
    

  return (
      <section className="blocked">
          
    </section>
  )
}

export default Blocked
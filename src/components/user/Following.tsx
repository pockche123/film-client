import React from 'react'
import { IUser } from '../../interfaces/IUser'
import './Following.css'

const Following = ({data}: {data:IUser| undefined}) => {
  return (
      <section className="following">
          
    </section>
  )
}

export default Following